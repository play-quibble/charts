Quibble has been deployed!

{{- if .Values.ingress.enabled }}

Your app will be available at:
  https://{{ .Values.ingress.host }}

Players join at the root URL. Hosts sign in at /login.
{{- else }}

Ingress is disabled. Forward ports to access locally:

  kubectl port-forward svc/{{ include "quibble.fullname" . }}-web 3000:3000 -n {{ .Release.Namespace }}
  kubectl port-forward svc/{{ include "quibble.fullname" . }}-api 8080:8080 -n {{ .Release.Namespace }}

Then open http://localhost:3000
{{- end }}

Prerequisites:
  - ingress-nginx must be installed in the cluster
  - cert-manager must be installed with a ClusterIssuer named "letsencrypt-prod"

To check pod status:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}
